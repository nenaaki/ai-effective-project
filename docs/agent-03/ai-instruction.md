# {作業名称}

- **{〇〇}**の一覧機能を作る
  - **機能の所在:** アプリケーション `app_xxxxx` 内の相対URL `/xxxxxxxx`

## 実装作業

- DBのテーブルの追加
  - **テーブル名:** {xxxxxx}
  - **スキーマファイル:** `テーブル名`.prisma を生成または更新する
  - テーブル構造は後述する
- **Backend実装:** `.graphql` ファイルの定義案を作成または更新し、その後にそれに対応する NestJS の Resolver と Prisma Schema を作成する
- **Frontend実装:** `機能の所在` にアクセスしたとき、`フロントエンド`の項目に記載された機能が動作しなければならない

### テーブル構造

- **{テーブル構造の情報を貼り付ける。CSVかTSVが望ましい}**
- **{MCPで読めるところにあるなら読み取り先の情報でもよい}**

### フロントエンド

- **画像からClaudeに指示できる（ポンチ絵のレベルも可）**
- **d2, mermaid などで簡潔な図を用意しても良い**
- 画面の仕様を記述。前述のAPIを何に使うのか記載。
- アトミックデザインを無理のない範囲で採用。再利用可能なものは再利用する。
